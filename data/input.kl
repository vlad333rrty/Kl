class Hello {
    // test function
    static fun f(int x, y) -> string {
        if x + 1 < y {
            ret "Wrong data!"
        }
         do {
            println(x, y)
            x = x - 1
         } while x + 1 >= y
         println("finish")
         ret "Success"
    }

    begin {
       println(f(4,1))
    }
}

